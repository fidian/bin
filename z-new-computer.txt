# In the terminal
# * change the color theme to white on black
# * Remove the menubar
# * Turn off terminal bell in the profile
# * Increase scrollback to 100,000 lines in the profile

sudo add-apt-repository main
sudo add-apt-repository universe
sudo add-apt-repository restricted
sudo add-apt-repository multiverse
sudo sed -i.bak "/^# deb .*partner/ s/^# //" /etc/apt/sources.list
sudo apt update
sudo apt install vim ubuntu-restricted-extras gnome-tweak-tool gnome-session

# Restart system. When logging in, click on username, then click on gear.
# Select GNOME on Xorg to get vanilla Gnome.

sudo apt install git editorconfig curl mmv
ssh-keygen

# Open Firefox, download and install Chrome https://chrome.google.com/
# Open Chrome, sign in.
# Go to github.com, login, add new SSH key.

git clone git@github.com:fidian/bin.git
cd bin
gpg --import Tyler\ Akins.secret.asc
gpg --edit-key 8F3B8C432F4393BD

# Type in: trust
# Type in: 5
# Type in: y
# Type in: quit

git submodule init
git submodule update
./setup_conf
cp ~/.ssh/id_rsa.pub ssh/$(hostname).pub
vim ssh/computers.txt

# Update the list of computers. Delete any old SSH keys.

git add ssh chrome-gnome-shell keepassx
git commit -m 'Updating SSH keys'
git push

# Run tweak tool and
# * Set the theme to Adwaita-dark
# * Turn on Ubuntu appindicators
# * Set monospace font to Anonymous Pro Regular 12
# * Enable compose key and set it to caps lock
# * Turn on the date in the toolbar

# Go to https://extensions.gnome.org/ and install
# * Places Status Indicator
# * AlternateTab
# * Applications Menu
# * Launch New Instance

sudo apt-get install apt-transport-https ca-certificates curl gnupg lsb-release
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
echo "deb [arch=amd64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io
sudo groupadd docker
sudo usermod -aG docker "$USER"
# Move docker off of ZFS - https://github.com/moby/moby/issues/41055
sudo systemctl stop docker
sudo rm -rf /var/lib/docker
sudo zfs create -V 20G rpool/docker
sudo mkfs.ext4 /dev/rpool/docker
sudo mkdir -p /var/lib/docker
sudo mount /dev/rpool/docker /var/lib/docker
sudo tail -1 /etc/mtab | sudo tee -a /etc/fstab
sudo systemctl start docker
# Log out and in again

sudo add-apt-repository ppa:heyarje/makemkv-beta
sudo apt install makemkv-bin makemkv-oss ccextractor

curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.34.0/install.sh | bash
# Manually remove lines from end of ~/.bashrc
# Close terminal and open a new one
nvm install 10
nvm use 10
nvm alias default 10

# Go to terminal preferences
# Disable F10 for accessing the menu
# Go to the unnamed profile
# Disable terminal bell
# Colors -> disable using colors from system and pick white on black
# Colors -> use Linux console palette, show bold in bright
# Scrolling -> limit scrollback to 100000 (add a zero)

# Launch Startup Applications
# Add: Resilio Sync, bin/resilio-sync
# Log out and log in
# Edit .resilio-sync/sync.conf and remove the line: "allow_empty_password": false,
bin/resilio-sync
# Set up Resilio Sync. Add new folders by making them first and restarting the container
for d in Sample Shared Folder Whatever; do mkdir -p "$d/.sync"; done
bin/resilio-sync
# Link up the keys

# Disable tracker
# https://www.linuxuprising.com/2019/07/how-to-completely-disable-tracker.html
